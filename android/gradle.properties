# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Increased heap size for R8/ProGuard builds (prevents OOM and speeds up processing)
# Use 4GB to avoid R8 "Java heap space" OOM during minifyReleaseWithR8
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# Enable parallel builds for faster compilation
org.gradle.parallel=true
# Enable build cache for faster incremental builds
org.gradle.caching=true
# Enable daemon for faster subsequent builds
org.gradle.daemon=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# Optimized: Only build for arm64-v8a (covers 99%+ of modern devices, reduces APK size by 30-50%)
reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Disable GIF support - not used in app, saves ~200KB
expo.gif.enabled=false
# Enable webp support in React Native images (~85 KB increase) - helps with image optimization
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false


# Enable ProGuard/R8 code minification and obfuscation (reduces APK size by 20-30%)
android.enableProguardInReleaseBuilds=true

# R8 optimization settings for faster builds
# R8 is faster than ProGuard and is enabled by default in AGP 3.4.0+
# Full mode provides better optimization but is MUCH slower
# Set to false for faster builds (slight size increase ~2-5%)
android.enableR8.fullMode=false

# Enable resource shrinking (reduces APK size by 5-15%, requires ProGuard)
# DISABLED: Very slow, causes build to hang. Enable only if needed.
android.enableShrinkResourcesInReleaseBuilds=true

# Enable bundle compression for additional size reduction
# DISABLED: Causes Node.js ReadableStream errors
android.enableBundleCompression=true
# Whether the app is configured to use edge-to-edge via the app config or `react-native-edge-to-edge` plugin
expo.edgeToEdgeEnabled=true